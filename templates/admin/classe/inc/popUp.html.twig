<div class="form-group">
    <ul>
        <li>
            <p>Nom: <span>{{professeur.name}}</span></p>
        </li>
        <li>
            <p>Prenom: <span>{{professeur.prenom}}</span></p>
        </li>
    </ul>
    <label class="col-md-3 control-label required" for="appbundle_theme_discipline" style="font-weight: bold">Classe</label>
    <div class="col-md-9 chooseClass">
        <select id="selectEtablissement"  class="form-control input-sm ">
            <div style="height: 200px; overflow-y: scroll ">
            <option>Choisir une classe</option>
            {%for classe in classes %}
                <option value="{{classe.id}}">{{classe.designation}}</option>
            {% endfor %}
            </div>
        </select>
        <button type="button" class="btn btn-secondary addProfesseurClasse" data-idProfesseur='{{professeur.id}}'>ajouter</button>
    </div>
</div>
<table style="margin-top: 2rem;" class="table table-bordered classeProfesseur">
  <thead>
    <tr>
      <th scope="col" style="background-color: #333; color: white;">Order</th>
      <th scope="col" style="background-color: #333;color: white;">Designation</th>
      <th scope="col" style="background-color: #333;color: white;">Action</th>
    </tr>
  </thead>
  <tbody id="tbodyAffectation">
    {% for i, classe in professeur.classe %}
        <tr>
            <td> {{ i + 1}} </td>
            <td> {{ classe.designation}} </td>
            <td>  <i  class='bi bi-dash-circle desafecterProfesseur' data-idClasse='{{classe.id}}' data-idProfesseur='{{professeur.id}}'></i> </td>

        </tr>
    {% endfor %}
  </tbody>
</table>